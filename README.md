# krita-update-checker
Krita plugin to check for latest versions.

# Install

If on Windows, run `install.bat`, otherwise manually move the contents inside `plugin/` to where ever `krita/pykrita` is.
